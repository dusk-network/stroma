version: "2"

# Baseline meta-linter configuration per
# nanto/knowledge/wiki/themes/project-quality-guidelines.md §"Language-Specific: Go".

linters:
  default: none
  enable:
    - errcheck
    - gocritic
    - gosec
    - goconst
    - gocyclo
    - ineffassign
    - misspell
    - prealloc
    - unconvert
    - unparam
    - revive
  settings:
    gocyclo:
      # The Rebuild orchestrator in index/index.go currently scores 38 against
      # a guideline baseline of 20. Refactoring it into named phases per
      # guideline §3.2 is tracked as follow-up; keeping the limit at 40 here
      # preserves the gate for new code without blocking the lint rollout.
      min-complexity: 40
    gocritic:
      enabled-tags:
        - diagnostic
        - performance
        - style
      disabled-checks:
        # Record is part of the public API (corpus.Record) and passed by value
        # through Normalized/Validate/HashRecord. Switching to pointer receivers
        # is a public API change, and these paths are not hot (per-record at
        # index build time, not per-chunk). Revisit if profiling justifies it.
        - hugeParam
  exclusions:
    generated: lax

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
